export enum tagTypes {
    user = "user",
    item = "item",
    orders = "orders",
    meal = "meal",
}

export const tagTypesList = [
    tagTypes.user,
    tagTypes.item,
    tagTypes.orders,
    tagTypes.meal,

]