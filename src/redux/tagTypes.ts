export enum tagTypes {
    user = "user",
    item = "item",
    order = "order",
    meal = "meal",
}

export const tagTypesList = [
    tagTypes.user,
    tagTypes.item,
    tagTypes.order,
    tagTypes.meal,

]